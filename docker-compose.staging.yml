services:
  app:
    build: .
    image: house-price:staging
    container_name: house-price-staging
    ports: ["8502:8501"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8501/"]
      interval: 30s
      timeout: 5s
      retries: 3
